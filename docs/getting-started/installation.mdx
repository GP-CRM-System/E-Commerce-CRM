---
title: Installation
description: Get started with Briefly CRM by installing and running the project locally.
---

# Installation

## Prerequisites

- [Bun](https://bun.sh) (v1.0.0 or higher)
- [PostgreSQL](https://www.postgresql.org/)
- [Redis](https://redis.io/) (for background jobs)

## Setup Steps

### 1. Clone the Repository

```bash
git clone https://github.com/seifsheikhelarab/E-Commerce-CRM.git
cd E-Commerce-CRM
```

### 2. Install Dependencies

```bash
bun install
```

### 3. Configure Environment Variables

```bash
cp .env.example .env
```

Edit `.env` with your database and authentication credentials. See [Configuration](/getting-started/configuration) for details.

### 4. Initialize the Database

```bash
bun run generate
prisma migrate dev
bun run seed
```

### 5. Start the Development Server

```bash
bun run dev
```

The API will be available at `http://localhost:3000`.

## Testing

Run the test suite:

```bash
bun test
```

## Code Quality

```bash
bun run lint    # Check for linting issues
bun run format  # Auto-fix formatting with Prettier
```
